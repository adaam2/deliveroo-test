import { create } from '@storybook/theming';

// You can find more configuration options for different
// parts of the UI at the link below:
// https://storybook.js.org/docs/configurations/theming

export default create({
  base: 'light',
  appBg: 'black',
  appContentBg: 'white',
  brandTitle: 'Deliveroo',
  textColor: 'white',
});